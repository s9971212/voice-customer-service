[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "voice-customer-service"
version = "0.1.0"
description = "A tool for Voice Customer Service"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [

]

[tool.setuptools.packages.find]
where = ["src"]
